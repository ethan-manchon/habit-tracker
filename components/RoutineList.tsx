"use client";
import React from "react";
import RoutineCard from "./RoutineCard";

type Props = {
    userId?: string;
};

export default function RoutineList({ userId }: Props) {
    return (
    <>
    
    </>
    );
}